.card-area
  // ko if: nextMovie() != null
  .video(data-bind="visible: !nextMovie().showDetails(), with: nextMovie()")
    include ./video-player
  .details(data-bind="visible: nextMovie().showDetails(), with: nextMovie()")
    include ./video-liked
  // /ko